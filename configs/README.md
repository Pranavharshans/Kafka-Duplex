# Configs

Configuration files for training, data generation, inference, and evaluation live here.

Expected future contents:

- model configs
- dataset configs
- training hyperparameter configs
- inference runtime configs
