# Data

Dataset assets and generated artifacts are organized here.

- `raw/` stores downloaded or source datasets
- `interim/` stores chunked or partially processed artifacts
- `processed/` stores finalized training-ready outputs

Large datasets should not be committed unless intentionally versioned.
